# 🐙 Mnsx-KrakenPool
> 基于纯 C++11 标准手写的高性能线程池，摒弃 pthread，利用 std::future 与可变参数模板实现安全的异步任务调度，为后端项目提供强大的并发“心脏”

## 📌 项目简介

**Mnsx-KrakenPool** 是基于 C++11 标准开发的高效线程池组件，本项目完全脱离传统的 POSIX 线程库，深入使用 `std::thread` 管理线程声明周期，结合 `std::mutex` 和 `std::conditon_variable` 实现核心的生产者-消费者模型

本项目是在学习完 C++11 现代开发特性后的练习，后续会成为其他 C++ 后端项目（如数据库连接池、文件传输系统）的并发核心

## 🚀 核心特性

* **纯 C++11 实现**：无任何第三方依赖，极简且易于集成
* **RAII 资源管理**：通过构造函数分配资源，析构函数阻塞等待工作线程（ Join ），杜绝内存泄漏和死锁
* **防御性编程**：严格的生命周期状态校验，禁止拷贝和赋值操作，防止其他组件误用，explicit 修饰构造函数杜绝隐式类型转换
* **泛型任务支持**：利用**可变参数模板**与**完美转发**，支持提交任意签名的函数或Lambda表达式
* **异步结果追踪**：结合 `std::packaged_task` 与 `std::future`，主线程可以无阻塞的提交任务，并且在需要时通过 `future.get()` 异步的获取返回结果
* **类型擦除技术**：通过 `std::bind` 与 `std::function` 实现复杂的类型擦除，将不同类型的任务统一存入任务队列
* **线程安全容器**：内置自定义 `SafeQueue` ，支持阻塞和非阻塞操作

## 🗺️ 开发路线图

项目采用分阶段迭代开发，目前处于第一阶段

* **[ X ]** **Stage 1：基础骨架**
  * 创建核心工作线程和任务队列
  * 基于条件变量控制的任务调度机制
  * 支持基础无参数、无返回值的 `void()` 任务提交
* **[ X ]** **Stage 2：泛型支持和异步返回**
  * 引入可变参数模板和完美转发实现泛型
  * 使用 `std::packaged_task` 和 `std::future` 支持任意签名的函数的提交与返回值获取
* **[ X ]** **Stage 3：架构解耦和性能压缩**
  * 封装独立的线程安全容器 `SafeQueue`
  * 引入 `std::atomic` 原子变量，压缩状态切换开销
  * 重构 CMake 体系

* **[   ]** **Stage 4：稳定性增强**
  * 增加任务拒绝策略
  * 增加异常处理机制
* **[   ]** **Stage 5：扩展其他功能**


## 🛠️ 编译与运行

本项目使用 CMake 构建，可以通过以下命令运行基础测试 Demo：

```bash
mkdir build && cd build
cmake ..
make
./KrakenDemo
```
